name: Lint with Black

on:
  push:
    branches: [1.x]
  pull_request:
    branches: [1.x]

jobs:
  black:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: psf/black@stable
