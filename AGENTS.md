# Maintainer instructions

Update `docs/app-builder-help.html` in the same change as user-facing CLI, config, build, install, or release behavior.

Keep it as the only HTML file in `docs`. Do not add temporary audit or planning pages there.
